Write a program that:
• Prompts the user to enter a password.
• Checks the strength of the password based on the following
criteria:
– Minimum length of 8 characters.
– Contains at least one uppercase letter.
– Contains at least one lowercase letter.
– Contains at least one digit.
– Contains at least one special symbol (e.g., !, @, #, $, %).
• Uses a for loop to iterate through each character of the
password.
• Uses if-else statements (possibly nested) within functions to
Page 24
check each condition.
• Displays ”Strong Password” if all conditions are met; otherwise, display ”Weak Password” and indicate the missing
criteria.
